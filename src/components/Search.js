import React from 'react'

function Search(props) {
    return (
        <div>
            <input onChange={props.handleInput} type="text" name="" id="" placeholder="Search Notice"/>
        </div>
    )
}

export default Search
